<h1>Zespoły</h1>

<table>
  <tr>
    <th>Nazwa</th>
    <th>Pochodzenie</th>
    <th>Gatunek</th>
    <th>Rok</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @lists.each do |list| %>
  <tr>
    <td><%= list.nazwa %></td>
    <td><%= list.pochodzenie %></td>
    <td><%= list.gatunek %></td>
    <td><%= list.rok %></td>
    <td><%= link_to 'Show', list %></td>
    <td><%= link_to 'Edit', edit_list_path(list) %></td>
    <td><%= link_to 'Destroy', list, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New List', new_list_path %>
